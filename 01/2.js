function add(a, b) {
  return BigInt(a) + BigInt(b);
}

function multiply(a, b) {
  return BigInt(a) * BigInt(b);
}

function subtract(a, b) {
  return BigInt(a) - BigInt(b);
}

function divide(a, b) {
  return BigInt(a) / BigInt(b);
}
